<div class=''>
	<label class='switch'>
		<input data-theme='dark' type='checkbox' id='slider' checked />
		<span data-theme='dark' class='slider'></span>
	</label>
</div>

<script>
	import '../js/dark_mode.js';
</script>

<style lang='scss'>
	@use '../scss/base' as *;

	[data-theme='light'] {
		--primary: #2a83d6;
		--background: #fff;
		--text: #000;
	}

	[data-theme='dark'] {
		--primary: #e9833e;
		--background: #000;
		--text: #fff;
	}

	/* Estilos del switch */

	.switch {
		position: relative;
		display: inline-block;
		width: 60px;
		height: 34px;
	}

	.switch input {
		opacity: 0;
		width: 0;
		height: 0;
	}

	.slider {
		position: absolute;
		cursor: pointer;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-color: $Dark-BG-Card;
		-webkit-transition: 0.4s;
		transition: 0.4s;
		border-radius: 34px;
	}

	.slider:before {
		position: absolute;
		content: '';
		height: 26px;
		width: 26px;
		left: 4px;
		bottom: 4px;
		background-color: $Light-BG-Card-hover;
		-webkit-transition: 0.4s;
		transition: 0.4s;
		border-radius: 50%;
	}

	input:checked + .slider {
		background-color: $Dark-BG-Card;
		background-color: $Dark-title-color;
	}

	input:checked + .slider:before {
		-webkit-transform: translateX(26px);
		-ms-transform: translateX(26px);
		transform: translateX(26px);
	}
</style>
